{
  "name": "foundry-clipboard-buddy",
  "version": "0.5.0",
  "description": "Paste media and contextual text from Clipboard into Tiles, Tokens, Notes, or Chat",
  "main": "clipboard-image.js",
  "scripts": {
    "copy": "rm -rf dist && mkdir -p dist && cp ./clipboard-image.js dist/ && cp ./clipboard-image.css dist/ && cp ./README.md dist/ && cp ./TESTING.md dist/ && cp ./CHANGELOG.md dist/ && cp ./LICENSE.md dist/ && cp ./module.json dist/ && cp -R ./templates dist/",
    "copyWin": "if exist dist rmdir /S /Q dist && mkdir dist && copy module.json dist && copy clipboard-image.js dist && copy clipboard-image.css dist && copy README.md dist && copy TESTING.md dist && copy CHANGELOG.md dist && copy LICENSE.md dist && xcopy templates dist\\templates /E /I /Y",
    "lint": "eslint .",
    "zip": "npm-build-zip --source=dist --destination=.",
    "test": "npm run test:unit",
    "test:unit": "npm run test:unit:run && node scripts/check-coverage.js",
    "test:unit:run": "vitest run --coverage",
    "test:unit:watch": "vitest",
    "test:smoke": "playwright test",
    "test:headed": "playwright test --headed",
    "test:install": "playwright install chromium",
    "test:list": "playwright test --list"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sayhiben/foundry-clipboard-buddy.git"
  },
  "keywords": [
    "foundry",
    "vtt",
    "clipboard",
    "media",
    "image",
    "video",
    "gif",
    "webp",
    "text",
    "notes",
    "paste",
    "tile",
    "token",
    "chat"
  ],
  "author": "JeansenVaars (Saif Ellafi)",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/sayhiben/foundry-clipboard-buddy/issues"
  },
  "homepage": "https://github.com/sayhiben/foundry-clipboard-buddy",
  "devDependencies": {
    "@eslint/js": "^9.23.0",
    "@playwright/test": "^1.54.2",
    "@vitest/coverage-v8": "^3.2.4",
    "eslint": "^9.23.0",
    "globals": "^16.0.0",
    "jsdom": "^26.0.0",
    "npm-build-zip": "^1.0.3"
    ,"vitest": "^3.2.4"
  }
}
