{
  "name": "foundry-clipboard-buddy",
  "version": "0.5.0",
  "description": "Paste images from Clipboard into Tiles",
  "main": "clipboard-image.js",
  "scripts": {
    "copy": "mkdir -p dist && cp ./clipboard-image.js dist/ && cp ./README.md dist/ && cp ./CHANGELOG.md dist/ && cp ./LICENSE.md dist/ && cp ./module.json dist/ && cp ./THANKS.txt dist/",
    "copyWin": "mkdir dist && copy module.json dist && copy clipboard-image.js dist && copy README.md dist && copy CHANGELOG.md dist && copy LICENSE.md dist && copy module.json dist && copy THANKS.txt dist",
    "zip": "npm-build-zip --source=dist --destination=.",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sayhiben/foundry-clipboard-buddy.git"
  },
  "keywords": [
    "foundry",
    "vtt",
    "clipboard",
    "image",
    "paste",
    "tile"
  ],
  "author": "JeansenVaars (Saif Ellafi)",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/sayhiben/foundry-clipboard-buddy/issues"
  },
  "homepage": "https://github.com/sayhiben/foundry-clipboard-buddy",
  "devDependencies": {
    "npm-build-zip": "^1.0.3"
  }
}
